# Groq API Key for summarization
GROQ_API_KEY=your_groq_api_key_here

# Redis connection (Optional, will fallback to memory if not provided)
REDIS_URL=redis://localhost:6379

# Environment
NODE_ENV=development
PORT=3000
